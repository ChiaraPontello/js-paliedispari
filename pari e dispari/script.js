// Pari e Dispari

// pari
function pari (number) {
    return number % 2 === 0;
  }
  // dispari 
  function dispari(number) {
    return number % 2 !== 0;
  }
  // chiedere se il numero scelto è pari o dispari 
  let miaScelta = prompt('pari o dispari?');
  console.log('la tua scelta è stata:' + ' ' + miaScelta);
  
  
